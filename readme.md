# YouTube Live EPG Generator

This repository generates a demo XMLTV EPG for a YouTube Live feed.

## Usage

1. Edit `epg_generator.sh` to configure output path and number of days.

2. Run the script:

```bash
chmod +x epg_generator.sh
./epg_generator.sh
```
